# Deno FFI 原生模組筆記

## 載入方式

原生模組透過 `Deno.dlopen` 載入共享物件。載入時需要 `--allow-ffi` 權限，而且共享物件
必須能被行程讀取。

## 字典檔

內附的字典檔與原始碼放在同一棵目錄樹，並以模組本身為基準解析路徑，因此在任何工作
目錄下都能載入。

## 失敗處理

載入失敗時只記錄一次錯誤，並降級到備援路徑。備援路徑讓服務繼續運作。
