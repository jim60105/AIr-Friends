name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write
  id-token: write
  attestations: write

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Cache Deno dependencies
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/deno
            ~/.deno
          key: ${{ runner.os }}-deno-${{ hashFiles('deno.lock') }}
          restore-keys: |
            ${{ runner.os }}-deno-

      - name: Run all tests
        run: deno task test

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Extract changelog content
        id: extract_changelog
        run: |
          # Extract the latest version's changelog content
          VERSION="${{ github.ref_name }}"
          # Remove 'v' prefix if present
          VERSION_NUM="${VERSION#v}"

          # Create installation guide
          echo "## Container Image" > release_notes.md
          echo "" >> release_notes.md
          echo "This release is published to multiple container registries:" >> release_notes.md
          echo "- GitHub Container Registry (ghcr.io)" >> release_notes.md
          echo "- Docker Hub" >> release_notes.md
          echo "- Quay.io" >> release_notes.md
          echo "" >> release_notes.md
          echo "You can pull the image using Podman:" >> release_notes.md
          echo "" >> release_notes.md
          echo '```bash' >> release_notes.md
          echo "podman pull ghcr.io/${{ github.repository_owner }}/air-friends:${VERSION_NUM}" >> release_notes.md
          echo '```' >> release_notes.md
          echo "" >> release_notes.md
          echo "## Changelog" >> release_notes.md
          echo "" >> release_notes.md

          # Count lines before appending changelog
          LINES_BEFORE=$(wc -l < release_notes.md)

          # Find the section for this version and extract content until next version or end
          awk "/^## \[$VERSION_NUM\]/{flag=1; next} /^## \[/{if(flag) exit} flag" CHANGELOG.md >> release_notes.md

          # Count lines after appending changelog
          LINES_AFTER=$(wc -l < release_notes.md)

          # If no new lines were added, the changelog section wasn't found
          if [ "$LINES_BEFORE" -eq "$LINES_AFTER" ]; then
            echo "No changelog available for this version." >> release_notes.md
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          body_path: release_notes.md

  docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Extract version from tag
        id: extract_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Setup Docker
        id: setup
        uses: ./.github/workflows/docker-reused-setup-steps
        with:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_TOKEN: ${{ secrets.QUAY_TOKEN }}
          tag: ${{ steps.extract_version.outputs.VERSION }}

      - name: Build and push
        id: build_push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Containerfile
          push: true
          target: final
          tags: ${{ steps.setup.outputs.tags }}
          labels: ${{ steps.setup.outputs.labels }}
          build-args: |
            VERSION=${{ steps.extract_version.outputs.VERSION }}
            RELEASE=${{ github.run_number }}
          platforms: linux/amd64,linux/arm64
          # Cache from latest to speed up tagged builds
          cache-from: |
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/air-friends:cache
            type=registry,ref=ghcr.io/${{ github.repository_owner }}/air-friends:latest
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/air-friends:cache,mode=max
          sbom: true
          provenance: true

      - name: Add attestations for GitHub Container Registry
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: ghcr.io/${{ github.repository_owner }}/air-friends
          subject-digest: ${{ steps.build_push.outputs.digest }}

      - name: Add attestations for DockerHub
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: ${{ secrets.DOCKERHUB_USERNAME }}/air-friends
          subject-digest: ${{ steps.build_push.outputs.digest }}

      - name: Add attestations for Quay
        uses: actions/attest-build-provenance@v3
        with:
          subject-name: quay.io/${{ github.repository_owner }}/air-friends
          subject-digest: ${{ steps.build_push.outputs.digest }}
