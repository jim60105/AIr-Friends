## 1. Shared Payload Helper

- [x] 1.1 Add `skills/lib/payload.ts` exporting `resolvePayloadBase(cwd, sessionId)` → `{cwd}/tmp/{sessionId}` (falling back to `{cwd}/tmp` when sessionId is empty) and `resolvePayloadPath(payloadPath, cwd, sessionId)`: resolves against `cwd` (the script's working directory = session workspace), requires the result to be inside the session staging base using boundary-safe matching (equal-or-separator-prefixed, so `{base}-2`/`{base}2` siblings are rejected), and — when the file exists — re-checks `Deno.realPath()` containment so a symlink escaping the staging dir is rejected; raises typed errors carrying a stable `code` (`SKILL_PAYLOAD_OUT_OF_BOUNDS`, `SKILL_PAYLOAD_NOT_FOUND`) plus an instructive guidance message, or returns the resolved absolute path
- [x] 1.2 Add a `readPayloadArg(args, flagName, opts?)` helper: rejects any `Deno.args` token matching `/^--(?:message|content|query|caption)(?:=|$)/` with code `SKILL_LEGACY_FLAG` and guidance that states the flag was removed for security (shell `$` expansion corrupts/leaks content), forbids message content on the command line, and shows the two-step payload-file flow with the correct example invocation; reads the `--<flag>-file` value, resolves via `resolvePayloadPath`, reads the file as UTF-8, and returns the content; raises `SKILL_MISSING_PAYLOAD` naming the required flag when a REQUIRED payload flag is missing (optional payloads return `undefined` when omitted)
- [x] 1.3 Extend `exitWithError` in `skills/lib/client.ts` to accept an optional `code` field and emit `{ success: false, code, error }` JSON on stderr (backward-compatible: code omitted for non-contract errors)
- [x] 1.4 Unit tests for `skills/lib/payload.ts`: own-session staging file accepted (content with `$0`, `$HOME`, newlines, empty string preserved verbatim); workspace-root file (`memory.private.jsonl`, `{workspace}/memory.private.jsonl`) rejected; sibling session dir (`{workspace}/tmp/sess_other/x`) rejected; home-anchored (`~/.git-credentials`, `$HOME/.env`) and absolute (`/etc/passwd`) rejected; prefix-sibling dirs (`{workspace}/tmp/sess_own-2/x`, `{workspace}/tmp/sess_own2/x`) rejected; symlink escape rejected; missing file errors; missing required flag errors; legacy flags in both forms rejected — AND every error carries the expected `code` plus guidance that includes the correct example invocation (assert on substrings)

## 2. Skill Script Migrations

- [x] 2.1 `skills/send-reply/scripts/send-reply.ts`: replace `--message` with `--message-file` (keep `-m` alias mapping to the file flag), read content via the shared helper (passing a send-reply-specific example invocation for the guidance), pass `{ message }` to the API as before; shebang becomes `#!/usr/bin/env -S deno run --allow-net --allow-env --allow-read --allow-write` (--allow-write enables the best-effort deletion of the consumed payload file); best-effort delete the payload file after a successful read
- [x] 2.2 `skills/edit-reply/scripts/edit-reply.ts`: same migration for `--message` → `--message-file` (`--message-id` unchanged), shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)
- [x] 2.3 `skills/set-reminder/scripts/set-reminder.ts`: `--message` → `--message-file`, shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)
- [x] 2.4 `skills/send-file/scripts/send-file.ts`: `--caption` → `--caption-file` (OPTIONAL; pass `undefined` when omitted), `--file-path` unchanged, shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)
- [x] 2.5 `skills/memory-save/scripts/memory-save.ts`: `--content` → `--content-file`, shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)
- [x] 2.6 `skills/memory-search/scripts/memory-search.ts`: `--query` → `--query-file`, shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)
- [x] 2.7 `skills/fetch-context/scripts/fetch-context.ts`: `--query` → `--query-file` (OPTIONAL), `--type` unchanged, shebang gains `--allow-read` and `--allow-write` (for best-effort payload deletion)

## 3. ACP Permission Gate

- [x] 3.1 In `src/acp/client.ts`, add `resolveSessionPath(path)`: replaces the exact tokens `$TMPDIR`/`${TMPDIR}` with the resolved session TMPDIR (`resolve(config.workingDir, "tmp")`) and `$SESSION_ID`/`${SESSION_ID}` with `config.sessionId` (empty string when absent); other `$VAR` tokens untouched; returns the canonical path
- [x] 3.2 Apply `resolveSessionPath()` in `isPathAllowed()`, `isAgentWorkspacePath()`, and `isWithinTmpDir()` (covering the scoped edit/write approval branch) before the boundary-safe checks
- [x] 3.3 `readTextFile()` and `writeTextFile()` SHALL perform their actual `Deno.readTextFile`/`Deno.writeTextFile` on the `resolveSessionPath()`-canonicalized path (NOT the raw `params.path`), so the approved path is the accessed path (no literal `$TMPDIR` directory under the bot's cwd)
- [x] 3.4 In the skill-command auto-approval branch of `requestPermission()` (kind `execute`, script/prefix match), reject the command when any whitespace-delimited token matches `/^--(?:message|content|query|caption)(?:=|$)/`; write a `permission_denied` audit entry with reason `rejected_skill_free_text_flag` and log the rejection; `--message-id`, `--message-file`, `--content-file`, `--query-file`, `--caption-file` tokens SHALL NOT trigger rejection
- [x] 3.6 `setupSession` pre-creates the session staging directory `{workspace}/tmp/{sessionId}` (parents are not created by the edit/write tool or `writeTextFile`); regression test asserts the dir exists mid-session and is a writable directory (review finding)
- [x] 3.7 `ClientConfig.sessionId` set for ALL session flows (fixed self-research and channel memory-maintenance client configs) so the gate expands `$SESSION_ID` consistently with the script-side `--session-id` (review finding)
- [x] 3.5 `requestPermission`-level tests in `tests/acp/client.test.ts`: edit/write with literal `$TMPDIR/$SESSION_ID/reply.md` and `${TMPDIR}/${SESSION_ID}/reply.md` approved; `$TMPDIR2/x` and `$OTHER/x` rejected; `writeTextFile` with `$TMPDIR/$SESSION_ID/reply.md` writes the EXPANDED path (assert no `$TMPDIR` directory created under the test cwd and the content lands in `{workingDir}/tmp/{sessionId}/reply.md` verbatim); skill command with `--message "text"` AND `--message=text` denied while `--message-id`/`--message-file` commands approved

## 4. SKILL.md, Prompts, and Documentation

- [x] 4.1 Update `skills/send-reply/SKILL.md`: two-step usage (write payload to `$TMPDIR/$SESSION_ID/reply.md` with the edit/write tool, then invoke directly — shebang form — with `--message-file "$TMPDIR/$SESSION_ID/reply.md"`), warning that message content MUST NOT appear on the command line, error-code reference table (`SKILL_LEGACY_FLAG` / `SKILL_MISSING_PAYLOAD` / `SKILL_PAYLOAD_OUT_OF_BOUNDS` / `SKILL_PAYLOAD_NOT_FOUND`) so the agent knows what its failure output means, parameter table update
- [x] 4.2 Update `SKILL.md` for `edit-reply`, `set-reminder`, `send-file`, `memory-save`, `memory-search`, `fetch-context` with the same two-step flow and payload-file flags (optional payloads marked optional; error-code reference where applicable)
- [x] 4.3 Update `prompts/system_summary.md`: replace the embedded `memory-save --content "..."` usage block and `content:` parameter description with the payload-file flow (the summary text is written to `$TMPDIR/$SESSION_ID/summary.md` and passed via `--content-file`)
- [x] 4.4 Repository-wide search for remaining legacy free-text call examples (`--content "`, `--message "`, `--query "`, `--caption "` in prompts/, docs/, skills/) and update `docs/SKILLS_IMPLEMENTATION.md` and `AGENTS.md` accordingly (note the shell-expansion rationale in the docs)
- [x] 4.5 Verify `prompts/agent_permissions.md` already lists `$TMPDIR` as writable scratch (it does) — no change unless wording needs the payload-file flow mention

## 5. Discord Startup Command Log Wording

- [x] 5.1 Rename `cleanupSlashCommands()` to `alignSlashCommands()` in `src/platforms/discord/discord-adapter.ts` (and its call site in the ready handler); update the method's JSDoc comment from "cleanup" to "align"
- [x] 5.2 Reword log messages: `"Deleting all global slash commands"` → `"Aligning all global slash commands"`, `"Successfully deleted all global slash commands"` → `"Successfully aligned all global slash commands"`, `"Failed to delete global commands"` → `"Failed to align global commands"`, `"Deleting slash commands for guild"` → `"Aligning slash commands for guild"`, `"Successfully deleted all guild commands"` → `"Successfully aligned all guild commands"`, `"Failed to delete guild commands"` → `"Failed to align guild commands"`
- [x] 5.3 Confirm no tests or other code reference the old strings or method name (grep for `deleted all`, `Deleting all`, `cleanupSlashCommands`)

## 6. Instructive Retry Prompt

- [x] 6.1 Rewrite `defaultRetryMessage` in `getRetryPromptStrategy` (`src/acp/agent-factory.ts`): state that the turn ended without a reply or reaction; list likely causes (legacy `--message` used and rejected; payload file never written; payload staged outside `$TMPDIR/$SESSION_ID/`; a previous `send-reply` call errored — read its output); give the correct two-step example invocation; keep embedding the full `send-reply` and `react-message` SKILL.md content
- [x] 6.2 Unit test for `getRetryPromptStrategy` (in the existing agent-factory test file, or new): assert the retry message contains the cause list, the `--message-file` example with `$TMPDIR/$SESSION_ID`, and the embedded SKILL.md content

## 7. Verification

- [x] 7.1 Subprocess integration tests (new, e.g. `tests/skills/scripts.test.ts`): execute `send-reply.ts`, `memory-save.ts`, and one optional-payload script (`send-file.ts` or `fetch-context.ts`) DIRECTLY (shebang form) with a staged payload and a mock `--api-url` HTTP server — assert `$0`/`$HOME`/newline/empty content reaches the API verbatim; legacy flags (both `--flag value` and `--flag=value`) exit non-zero, emit the `SKILL_LEGACY_FLAG` code and guidance containing the correct example, and never hit the API; payloads outside the session staging dir (workspace root, sibling session, symlink escape) exit non-zero with `SKILL_PAYLOAD_OUT_OF_BOUNDS`
- [x] 7.2 Run `deno fmt src/ tests/`, `deno lint src/ tests/`, `deno check src/main.ts`, and `deno task test`; confirm no regressions and coverage stays above 75%
- [ ] 7.3 Manual/live regression: run a restricted-mode session with a reply containing `$0.435` and `$HOME` and assert the literal text reaches the platform unchanged (also verify a legacy `--message` invocation errors with instructive guidance and the missing-reply retry recovers)
