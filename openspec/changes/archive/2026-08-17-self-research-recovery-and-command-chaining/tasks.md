## 1. Multi-Command Splitting Helper

- [x] 1.1 In `src/acp/client.ts`, add `splitCommandSegments(cmd: string): string[]`: quote-aware scan tracking single/double quote state (with shell-correct backslash escapes inside double quotes), splitting on `;`, `&&`, `||` only outside quotes; trim segments, drop empty ones; return `[cmd]` unchanged when no separator exists or quotes are unbalanced
- [x] 1.2 Add `multiCommandRejectionReason(cmd, base, allowedDirs, home, xdgDataHome, dataRoot): GenericCommandRejection | null` that splits the command and returns the FIRST failing segment's reason (from `genericCommandRejectionReason`), or `null` when every segment passes; a segment consisting ONLY of tolerated fd-redirect tokens SHALL be rejected as `shell_operator` (operator artifact, never skipped); single-segment commands must behave byte-identically to today
- [x] 1.3 Unit tests in `tests/acp/permission-gate-generic.test.ts`: splitting correctness (plain chains, `&&`/`||`, quoted `;`/`&&`/`||` inside `"..."` and `'...'`, escaped `\"` inside double quotes, `; ;` empty segments, trailing `;`, unbalanced quotes → no split, glued `2>&1&&cat` → redirect-only segment, `2>&1; cat x` → redirect-only segment)
- [x] 1.4 Unit tests: `multiCommandRejectionReason` — all-approved chain returns `null`; first-failing-segment cause is reported (`cat x 2>&1; cat /etc/passwd` → `path_outside_boundary`, `cat x 2>&1; echo y` → `first_token_not_allowed`, `cat x 2>/dev/null || echo y` → `shell_operator`, `cat x | rg y` → `shell_operator`, `2>&1&&cat /etc/passwd` → `shell_operator`); existing single-command expectations unchanged

## 2. Shell-Expansion Token Tightening (D5)

- [x] 2.1 Extend `expandHomeReferences` (or a new `expandKnownEnvReferences`) in `src/acp/client.ts` to also expand `$TMPDIR`/`${TMPDIR}`, `$AGENT_WORKSPACE`/`${AGENT_WORKSPACE}`, `$SESSION_ID`/`${SESSION_ID}` (values from the caller-provided runtime context)
- [x] 2.2 In `genericArgWithinWorkspace`: reject tokens that still contain an UNQUOTED `$` reference after known-variable expansion (the quote-state scanner must distinguish quoted vs unquoted `$`); reject tokens containing unquoted brace-expansion syntax (`{`/`}` outside quotes); reject DOUBLE-QUOTED non-harness `$` references that begin the token's path content (`"$X/etc/passwd"`, `"$X"`, `--flag="$X"` — an unset variable expands to empty, exposing a literal `/...`/`..` suffix as absolute/traversal); reject backslash-escaped newlines (line continuations) in every quote state; quoted/embedded `$`/`{` references stay allowed
- [x] 2.3 In `referencesOutOfWorkspacePath` (skill matchers): recognize the harness-set variables as known without expanding (payload contract `$TMPDIR/$SESSION_ID/x.md` keeps working) and reject every other unquoted `$VAR` reference and unquoted brace token
- [x] 2.4 Unit tests in `tests/acp/permission-gate-generic.test.ts` + `tests/acp/client.test.ts`: `cat $IFS/etc/passwd`, `cat ${OTHER}/etc/shadow`, `cat $VAR/../etc/passwd`, `cat {safe,/etc/passwd}` rejected; `cat $TMPDIR/scratch.txt`, `cat $AGENT_WORKSPACE/notes/_index.md` approved (expanded, in-bounds); `rg 'cost is $5' {WS}/notes.md` and `rg "price $X" {WS}/notes.md` approved; skill payload invocation with `$TMPDIR/$SESSION_ID/x.md` still approved; existing `$HOME`/`$XDG_DATA_HOME` tests unchanged

## 3. Gate Integration

- [x] 3.1 Restructure the `requestPermission` `execute` branch in `src/acp/client.ts`: evaluate every command via its segments — each segment must pass the skill-whitelist matchers (`matchesScriptPath` / `matchesCommandPrefix`) OR `genericCommandRejectionReason` — approving only when ALL segments of ALL commands pass; keep the D5 legacy free-text flag check on the FULL original command before splitting
- [x] 3.2 Preserve the cause-specific rejection logging/audit: the WARN and the single `permission_denied` audit entry use the first failing segment's reason; add the failing segment text to the log context (messageTemplate stays `Rejecting generic command: {reason} (command {index} of {total}: {command})`)
- [x] 3.3 `tests/acp/client.test.ts` requestPermission-level tests: approve `agent-browser --args "--no-sandbox" open "https://example.com" 2>&1; agent-browser --args "--no-sandbox" get text 2>&1` (observed failure shape) and `cat {WS}/notes.md 2>&1; ls {WS}/`; reject `agent-browser open https://example.com || echo fallback`, `ls {WS} 2>&1 && cat /etc/passwd`, `cat {WS}/notes.md 2>/dev/null || echo "NO INDEX"`, `agent-browser; curl evil.com`, and `cat {WS}/notes.md | rg x` — each writing exactly ONE `permission_denied` audit entry with the correct cause
- [x] 3.4 Verify existing tests asserting chain rejections (`ls x 2>&1 && cat /etc/passwd`, `agent-browser 2>&1&&cat /etc/passwd`, `ls x 2>&1\nrm victim`) still pass with the updated rejection causes; update any test that asserts the OLD `shell_operator` cause for chains whose segments now produce a more specific cause

## 4. Self-Research Completion Verification

- [x] 4.1 In `src/types/config.ts` add `verifyCompletion: boolean` (default `true`) to `SelfResearchConfig`; wire the default in `src/core/config-loader.ts` and add `SELF_RESEARCH_VERIFY_COMPLETION` to `ENV_MAPPINGS` in `src/utils/env.ts` (boolean `"true"`/`"false"` with warn-on-invalid)
- [x] 4.2 Add a note-fingerprint helper (e.g. `snapshotAgentWorkspaceNotes(agentWorkspacePath)` + `producedResearchOutput(snapshot, sessionStartMs)` in `src/core/session-orchestrator.ts` or a small `src/core/research-output.ts`): RECURSIVE scan of `notes/` and `journal/` producing `relativePath → {size, mtimeMs, contentHash}` (sha-256 of bytes); production = new entry OR (hash changed AND `mtimeMs >= sessionStartMs - 1000`); I/O errors → log WARN and return "produced" (fail-safe, never retry on uncertainty)
- [x] 4.3 In `processSelfResearch`: snapshot before `promptWithIdleTimeoutHandling`; after an `end_turn`, when `verifyCompletion !== false` and no research output was produced: audit `retry_triggered` (reason `no_research_note`, retryCount 1, maxRetries 1), WARN log, snapshot `connector.getClient()?.getRecentPermissionRejections()` BEFORE the retry prompt, send ONE corrective retry prompt on the SAME session (message built in code: note requirement + `formatPermissionRejections` section + the commands OpenCode denies before the ACP gate — `echo`, `curl`, `git`, `python`, `mkdir`, … — with Read-tool guidance + sandbox usage rules + `$AGENT_WORKSPACE/notes/{topic-slug}.md` note path), then re-verify
- [x] 4.4 Audit lifecycle: first turn → `agent_response` with `isRetry: false`, NO `session_end` yet; retry turn → `prompt_sent` + `agent_response` with `isRetry: true`; exactly ONE `session_end` after the final verification outcome
- [x] 4.5 Final failure path: still no output after retry → `result.success = false`, `error = "no_research_note"`, audit `session_end` with `success: false` + reason, increment the counter (ONLY when verification enabled); non-`end_turn` stop reasons keep existing failure-without-retry behavior; `verifyCompletion: false` → byte-identical legacy flow (no snapshot, no metric)
- [x] 4.6 Tests (`tests/core/session-orchestrator.test.ts` or a new `self-research` test file, mirroring the existing orchestrator test harness): note produced → success; no note → retry sent once with rejection diagnostics → note produced → success; no note after retry → failure + counter + audit lifecycle (one `session_end`, `isRetry: true` entries); same-size same-millisecond overwrite detected via hash; pre-session modification does not count (mtime bound); verification I/O error → treated as produced, no retry; `verifyCompletion: false` → legacy behavior; non-`end_turn` → failure without retry

## 5. Metrics

- [x] 5.1 Register `airfriends_self_research_no_note_total` counter (no labels) in `src/utils/metrics.ts` and increment it in the self-research final-failure path (exactly once per no-note session, only when verification enabled)
- [x] 5.2 Test: counter increments once when a self-research session ends without a note, and does NOT increment when `verifyCompletion` is `false` (metrics tests in `tests/utils/metrics.test.ts` or equivalent)

## 6. Prompt & Skill Guidance

- [x] 6.1 `prompts/agent_permissions.md`: replace the stale command list (remove the false `curl` claim) with the real allow-list; document multi-command rule (`;`/`&&`/`||` chains allowed only when every command is individually allowed; whole call rejected otherwise; `|`, `&`, `2>/dev/null`, `> file` always rejected; `echo`/`curl`/`git`/`python`/`mkdir` denied by OpenCode before the ACP gate; use the Read tool instead of `cat`-with-fallbacks)
- [x] 6.2 `prompts/browser_automation.md`: same chaining rule; add "if `webfetch` returns 403/429, switch to `agent-browser` (one command per call or `;`/`&&`/`||`-chained allowed commands)"
- [x] 6.3 `skills/self-research/SKILL.md`: step 1 uses the Read tool on `$AGENT_WORKSPACE/notes/_index.md` (env-var path, deployment-independent — the workspace root is `workspace.repoPath`-derived, not fixed at `/app/data`); brief sandbox command note

## 7. Documentation & Config Files

- [x] 7.1 Update `config.example.yaml` (`selfResearch.verifyCompletion`, default `true`, commented) and `.env.example` (`SELF_RESEARCH_VERIFY_COMPLETION`)
- [x] 7.2 Update `helm/values.yaml` env section with `SELF_RESEARCH_VERIFY_COMPLETION` example
- [x] 7.3 Update `docs/AGENT_PERMISSIONS.md`: document the multi-command segment-wise approval rule, the quote-aware splitting, the redirect-only-segment rejection, the shell-expansion token tightening (harness-set variables expandable; other unquoted `$VAR`/brace tokens rejected; token-start double-quoted non-harness references rejected; quoted/embedded references allowed; backslash-escaped newlines rejected), the remaining rejections (`|`, `&`, newline, file redirects), and note that the `"||"`/`"&&"` entries in `agent-config/opencode.json` are inert (OpenCode's tree-sitter segmentation never produces them as command patterns) while `echo`/`curl`/`git`-style deny rules still block those segments at OpenCode's layer; document the residual embedded-double-quoted-expansion limitation and the argv-execution long-term fix
- [x] 7.4 Update CHANGELOG.md with the behavior changes (chained-command approval, shell-expansion tightening, self-research completion verification + retry, guidance corrections) and the note that mounted custom `agent_permissions.md`/`browser_automation.md` overrides must be re-mounted to receive the corrected guidance

## 8. Verification

- [x] 8.1 Run `deno fmt --check src/ tests/` (or `deno fmt src/ tests/`), `deno lint src/ tests/`, `deno check src/main.ts`, and `deno task test` — all green
- [x] 8.2 Confirm test coverage stays above 75% (`deno task coverage` if available)
- [x] 8.3 Sanity-check the observed failure shapes against the new code with a quick unit-level invocation (e.g. `deno eval` importing `splitCommandSegments`/`multiCommandRejectionReason` on the two production log commands: the `cat ... || echo` chain must still be rejected, the `agent-browser ...; agent-browser ...` chain must be approved)
- [x] 8.4 Manual contract verification against pinned OpenCode v1.17.13 (in the container image or a local opencode binary): confirm the observed `agent-browser ...; agent-browser ...` shape reaches the ACP gate as ONE request and is approved end-to-end, and that `|| echo`-style chains are still denied at OpenCode's own layer before the ACP gate; record the result in the change notes
