# Tasks: memory-patch channel-scope support

## 1. Handler routing (src/skills/)

- [ ] 1.1 Add `scope?: string` to `MemoryPatchParams` in `src/skills/types.ts` (mirror the optional-string shape of `MemorySaveParams.scope` / `MemorySearchParams.scope`; English JSDoc noting channel-scope patching requires the F15 channel-write capability). Verify: `deno check src/main.ts`.
- [ ] 1.2 In `handleMemoryPatch` (`src/skills/memory-handler.ts`), resolve the scope with the save pattern verbatim (`memory-handler.ts:89-95`): `const scope = (params.scope ?? "user") as MemoryScope;` then reject `scope !== "user" && scope !== "channel"` with the exact save/search string `"Invalid 'scope' parameter. Must be 'user' or 'channel'"` (default applies only to an omitted value; an explicit bogus/null value is rejected). Keep all existing field validation and the "at least one field" check scope-independent and before routing. Verify: unit test in 3.6 for the invalid-scope rejection plus existing user-scope tests still pass.
- [ ] 1.3 Add the channel branch after the shared validation, replacing the unconditional `patchMemory()` call (design D2): reject `visibility` on channel scope with the visibility error string from design D2; then `context.canWriteChannelMemory !== true` → `"Not authorized to write channel memory in this session"` (verbatim from `handleMemorySave`); then `channelId`/`workspaceManager` guards ("Cannot patch channel memory: ..."); then `getOrCreateChannelWorkspace(context.workspace.components.platform, context.channelId)` → `memoryStore.patchChannelMemory(channelWorkspace, params.memory_id, patch)`. Success path: Message Template `logger.info`, `memoryOperationsTotal.labels("patch", "public").inc()`, return the user-path response shape plus `scope: "channel"`. Verify: `deno check src/main.ts`.
- [ ] 1.4 On the user path, catch the store's `"Memory not found: <id>"` error and return `success: false` with the enhanced message ending in the `--scope channel` retry hint (design D3); all other errors propagate unchanged through the existing catch. Verify: unit test in 3.4.

## 2. Skill CLI + docs (skills/memory-patch/)

- [ ] 2.1 `scripts/memory-patch.ts`: add `"scope"` to the `string:` flags list; validate `["user","channel"]` else `exitWithError("Invalid scope. Must be 'user' or 'channel'")`; set `params.scope` (copy the scope handling in `skills/memory-save/scripts/memory-save.ts:70-75`). While in this flag block, fix the pre-existing falsy-guard on decay (`if (args.decay)` silently drops `--decay 0`; change to `args.decay !== undefined` — the decay-patch SKILL.md example in 2.2 makes `--decay 0` a reachable agent call). Verify: `deno check skills/memory-patch/scripts/memory-patch.ts`; `--scope bogus` exits non-zero with the error.
- [ ] 2.2 `SKILL.md`: document `--scope user|channel` (default `user`) in usage with a channel decay-patch example; add capabilities bullet (patch channel-scoped memories, requires a session authorized for channel writes); extend Limitations: channel scope cannot patch `visibility` (channel memories are always public); add error guidance: "Memory not found" on a user-scope patch of a `scope: "channel"` search result means retry with `--scope channel`. Keep frontmatter description accurate. Verify: file reads match behavior implemented in 1.x.

## 3. Tests (tests/skills/)

- [ ] 3.1 In a new file `tests/skills/memory-patch-channel-scope.test.ts` reusing the `withHandler` temp-dir harness from `tests/skills/channel-memory-authz.test.ts`: save a channel memory via `handleMemorySave` (authorized), then patch it via `handleMemoryPatch` with `{ memory_id, scope: "channel", decay: 0.4 }` (decay 0.8 → 0.4, the incident scenario). Assert: result success with `scope: "channel"` in data; `loadChannelMemories` resolves `decay: 0.4`; a patch event line was appended to the channel JSONL (original memory line byte-unchanged). Verify: `deno test --allow-read --allow-write --allow-env tests/skills/memory-patch-channel-scope.test.ts`.
- [ ] 3.1b Core-pin interaction on the channel path: save a working-tier channel memory (decay 0.8, authorized), then patch `{ memory_id, scope: "channel", tier: "core", decay: 0.4 }`; assert success and that the resolved memory has `tier: "core"` with `decay: 1.0` (the store's core-pin is honored through the skill, per the `importance-decay` spec's "Patch to core decay ignored"). Verify: same test command.
- [ ] 3.2 Authorization rejection: `canWriteChannelMemory: false` + `scope: "channel"` → `success: false`, error `"Not authorized to write channel memory in this session"`, and the channel file gains no new event. Verify: same test command.
- [ ] 3.3 Visibility rejection: authorized `scope: "channel"` + `visibility: "private"` → `success: false` with an error naming channel memories / visibility, no patch event written. Verify: same test command.
- [ ] 3.4 User-scope not-found hint: patch a nonexistent ID with scope omitted → `success: false`, error contains `Memory not found` and `--scope channel`. Verify: same test command.
- [ ] 3.5 Channel-scope not-found: authorized `scope: "channel"` with an ID absent from the channel file and patch payload `{ enabled: false }` → `success: false`, error `Channel memory not found: <id>`. Verify: same test command.
- [ ] 3.6 Regression guards: existing `tests/skills/memory-handler.test.ts` patch tests (user scope, no `scope` param) pass unchanged, covering response shape `{ patchId, targetId, timestamp, changes }`; add one invalid-scope case (`scope: "guild"` → error `"Invalid 'scope' parameter. Must be 'user' or 'channel'"`). Verify: `deno test --allow-read --allow-write --allow-env tests/skills/memory-handler.test.ts tests/skills/memory-patch-channel-scope.test.ts`.

## 4. Project checks

- [ ] 4.1 Run scoped checks: `deno check src/main.ts`, `deno fmt src/ tests/`, `deno lint src/ tests/`, and the two memory skill test files. Verify: all green.
