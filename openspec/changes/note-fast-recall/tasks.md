## 1. Configuration

- [x] 1.1 Add `fastRecallNoteMaxResults`, `fastRecallNoteMaxTokens`, and provisional `noteMinRecallScore: 3.0` and `secondNoteRecallScore: 3.0` with validation, and document them in `config.example.yaml`. Verify with config-loader tests. — `MemoryRecallConfig` and `DEFAULT_RECALL_CONFIG` carry the four keys (`2`, `256`, provisional `3.0`/`3.0`); `config-loader.ts` validates the two counts as integers `>= 0` and the two scores as numbers `>= 0`, so an out-of-range or mistyped value fails with a `ConfigError`; `config.example.yaml` documents each key under `memory.recall`. `tests/core/config-loader.test.ts` asserts the four defaults, that a partial override keeps them, and that each of the six invalid shapes is rejected. The provisional note scores are replaced by the calibrated values in 3.3.

## 2. Fast Recall notes

- [x] 2.1 Add note Fast selection (thresholds, second-note rule, max results, 256-token budget independent of memories). Verify with tests: notes do not displace memories, low-confidence notes are omitted, and `fastRecallNoteMaxResults: 0` selects none. — `MemoryRetriever.selectFastNotes()` walks the notes only when the request carries an agent workspace, applies `noteMinRecallScore`, `secondNoteRecallScore` and the shared `secondResultRatio`, and fits the rendered entries into `fastRecallNoteMaxTokens` with `fitNoteBudget()`. The memory selection is untouched, so the budgets are independent. `tests/core/memory-recall/retriever.test.ts` covers both kinds selected together, the threshold boundary at the note's own score, `fastRecallNoteMaxResults: 0`, an exhausted note budget leaving the memories intact and vice versa, the second-note conditions, a note that does not fit the budget, a Fast pointer carrying no deep `chunks` with an excerpt of at most 162 characters, and no note search without an agent workspace. The obsolete "Fast Recall returns no notes" test is replaced.
- [x] 2.2 Render the note sub-section in `fast-recall.ts` (three-line entries, excerpt-only heading, a notes-only section when no memory is selected). Verify with rendering tests, including a check that the full file text is absent. — `RELEVANT_NOTE_HEADING`, `renderNoteEntry()` and `estimateNoteSectionTokens()`; `renderFastRecallSection(memories, notes)` appends the note sub-section after the memory ones and renders it alone when no memory was selected. `tests/core/memory-recall/fast-recall.test.ts` asserts the exact three lines, the `1.4k` token formatting, the title de-duplication against the heading path, the title prefix when the heading path does not start with it, the pointer-only entry, the heading's containment wording, the notes-only section, and the heading-charged-once budget. `tests/core/context-assembler.test.ts` additionally asserts that a marker line of the note body never reaches the formatted prompt.
- [x] 2.3 Pass `agentWorkspacePath` into the Fast Recall request in `assembleContext()`. Verify with a context-assembler test using a temporary workspace with one matching note, and a bound test (500 memories plus 50 notes stay within 512 + 384 + 192 + 256 tokens plus headings). — `assembleContext()` reads the parameter the session orchestrator already passes, forwards it, and stores the selected pointers as `fastRecallNotes` next to `fastRecall`; `formatContext()` renders them and charges them as mandatory tokens. Tests: a matching note renders its pointer (path, heading path, line range, quoted excerpt) with no memory sub-section and no note body, a note never displaces a memory, the path is forwarded, no workspace means no note section, and the 500-memory + 50-note fixture keeps each of the four budgets plus the combined body within `512 + 384 + 192 + 256`.

## 3. Calibration

- [x] 3.1 Add about 10 notes under `tests/fixtures/memory-recall/notes/` and labeled note queries, including negatives, to `queries.yaml`. Verify with the fixture validation test (every expected note exists). — 12 Traditional Chinese notes with mixed-script entities under `tests/fixtures/memory-recall/notes/`, and 16 note queries carrying a `note-` case tag with an `expectedNotes` label (`note-natural`, `note-mixed-script`, `note-hard-ranking`, `note-negative`, `note-negative-near-miss`). `tests/core/memory-recall/benchmark.test.ts` asserts that every expected note exists as a fixture note, that each note has a level-1 heading, that the note cases are all covered, that the note set has negatives, and that a query expecting a note carries a note case tag.
- [x] 3.2 Extend the benchmark with the note grid pass and a `notes` block in `metrics.json`, timeboxing tuning to 2 hours. Verify by running it with `--write` and checking that the note false-positive rate is at most 5%. — `scripts/memory-recall-benchmark.ts` materializes the notes into the fixture's agent workspace, captures the ranked notes, replays the note rule offline (`selectNotesFromCapture`), runs the same two-stage grid search over `noteMinRecallScore`/`secondNoteRecallScore` (`calibrateNotes`), verifies the model against the engine at the calibrated, boundary and wrong threshold pairs for both kinds, and records a `notes` block plus a note diagnostic report. Tuning took well under the 2-hour timebox. `--write` recorded a note false-positive rate of `0.0357` (2 of 56), at or below the 5% cap.
- [x] 3.3 Set the calibrated note thresholds in `config-loader.ts` and `config.example.yaml`, and extend the regression test to assert the note metrics. Verify that the test passes. — The calibrated `noteMinRecallScore: 11.5` and `secondNoteRecallScore: 9.25` replace the provisional values in `DEFAULT_RECALL_CONFIG` (which the config loader spreads) and in `config.example.yaml`; the recorded `notes` block in `metrics.json` carries them with Recall@1 `0.9091`, Recall@2 `1.0`, false-positive rate `0.0357` and `20.9643` average injected tokens. The regression test asserts the four note metrics exactly, the note false-positive count, the recorded thresholds against the defaults, that the note grid search finds them, and that neither threshold sits on a grid bound. The memory thresholds and the recorded memory metrics are unchanged (`6.75`, `6.5`, `0.9565`, `1.0`, `0.025`, `17.85`), because the memory metric set is the memory queries alone. Note entries render an absolute path, so the note tokens are measured at the documented production workspace root: the fixture's temporary root length varies by a character between runs and moved the recorded average before that normalization.

## 4. Docs and verification

- [x] 4.1 Document note pointers in Fast Recall in `docs/MEMORY_DESIGN.md`. Run `deno task ci` and verify that it passes. — `docs/MEMORY_DESIGN.md` §8 documents the independent note pass, its budget and off-switch, the heading and the three-line entry, and the excerpt-only containment; §13 documents the note thresholds, the note metric set, the recorded note metrics, the canonical measurement root and the grid-bound assertion. `AGENTS.md` gains the context-assembly row and the note sub-section. `deno task ci` starts with a repo-wide `deno fmt --check`, which fails on 260 files that are already unformatted on `master` (`docs/`, `prompts/`, `openspec/changes/archive/` and others), so it cannot pass for any change; the CI-equivalent gates were run and pass: `deno fmt --check src/ tests/`, `deno lint src/ tests/`, `deno check src/main.ts` and the full `deno test --allow-net --allow-read --allow-write --allow-env --allow-run --allow-ffi` (2173 passed, 0 failed, 2 ignored).
